{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/coresys-dev/releases/main/manifest.schema.json",
  "title": "CORE Tools Store Manifest",
  "description": "Consumed by the app launcher to list available apps in the store: icon, screenshots and description are fetched from this repo using each app's media paths. Media paths are rooted at the repo root (e.g. '/apps/<id>/icon/icon.png') and resolved against https://raw.githubusercontent.com/<repo>/main.",
  "type": "object",
  "required": ["schemaVersion", "repo", "apps"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "type": "integer", "const": 1 },
    "repo": {
      "type": "string",
      "description": "GitHub 'owner/name' slug. Media paths and releases are resolved against this repo.",
      "pattern": "^[^/\\s]+/[^/\\s]+$"
    },
    "updatedAt": { "type": "string", "format": "date-time" },
    "minLauncherVersion": {
      "type": "string",
      "description": "Minimum launcher version (semver, no leading 'v') allowed to use the store and install/update apps. A launcher older than this must be updated before it can install or update anything. Optional; absent means no floor.",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[0-9A-Za-z.-]+)?$"
    },
    "apps": {
      "type": "array",
      "items": { "$ref": "#/definitions/app" }
    }
  },
  "definitions": {
    "app": {
      "type": "object",
      "required": ["id", "name", "description", "media", "releaseTagPrefix", "platforms"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "description": "Stable, unique app id. Used as the folder name and the release tag prefix.",
          "pattern": "^[a-z0-9][a-z0-9-]*$"
        },
        "name": { "type": "string", "description": "Display name shown in the store." },
        "hidden": {
          "type": "boolean",
          "description": "When true, the launcher omits this app from the store list. Update checks for an already-installed copy still work via releaseTagPrefix. Optional; defaults to false."
        },
        "tagline": { "type": "string", "description": "Short one-line pitch shown in store listings." },
        "description": { "type": "string", "description": "Longer description shown on the app's detail page." },
        "category": { "type": "string" },
        "pricing": {
          "type": "string",
          "enum": ["", "free"],
          "description": "Set to \"free\" to mark the app as available for free. Empty or absent means the app is locked to a license."
        },
        "platforms": {
          "type": "array",
          "items": { "type": "string", "enum": ["windows", "macos", "linux"] }
        },
        "releaseTagPrefix": {
          "type": "string",
          "description": "Prefix of this app's GitHub release tags, e.g. 'core-v' matches tags like core-v1.4.2.",
          "pattern": "^[a-z0-9-]+-v$"
        },
        "homepage": {
          "type": "string",
          "description": "Optional link to the app's own marketing/product page (separate from the store description). Leave empty if none."
        },
        "changelog": {
          "type": "string",
          "description": "Plain-text changelog shown on the app's detail page (not a URL)."
        },
        "media": {
          "type": "object",
          "required": ["icon", "screenshots"],
          "additionalProperties": false,
          "properties": {
            "icon": {
              "type": "string",
              "description": "Path to the app icon/logo, rooted at the repo root, e.g. '/apps/<id>/icon/icon.png'. Resolved as https://raw.githubusercontent.com/<repo>/main<path>.",
              "pattern": "^/apps/[a-z0-9][a-z0-9-]*/.+"
            },
            "banner": {
              "type": "string",
              "description": "Optional path to the app's wide hero banner, rooted at the repo root, e.g. '/apps/<id>/banner/banner.png'. Same resolution rule as icon. Recommended: 1920x480 PNG (4:1), min 1200x300, keep key art/text within the centre 80% for safe cropping.",
              "pattern": "^/apps/[a-z0-9][a-z0-9-]*/.+"
            },
            "screenshots": {
              "type": "array",
              "description": "Presentation screenshots (3 recommended). Each item is either a repo-root path string (e.g. '/apps/<id>/screenshots/1.png') or an object with 'src' and an optional 'caption'. Same resolution rule as icon.",
              "items": {
                "oneOf": [
                  { "type": "string", "pattern": "^/apps/[a-z0-9][a-z0-9-]*/.+" },
                  {
                    "type": "object",
                    "required": ["src"],
                    "additionalProperties": false,
                    "properties": {
                      "src": {
                        "type": "string",
                        "description": "Repo-root path to the screenshot image. Same resolution rule as icon.",
                        "pattern": "^/apps/[a-z0-9][a-z0-9-]*/.+"
                      },
                      "caption": {
                        "type": "string",
                        "description": "Custom caption / tooltip shown for this screenshot in the store."
                      }
                    }
                  }
                ]
              },
              "minItems": 1
            }
          }
        }
      }
    }
  }
}
